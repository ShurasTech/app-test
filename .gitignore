# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment / secrets — never commit real webhook URLs (NFR2, AD-6)
.env
.env.*
!.env.example

# Capacitor iOS generated project (regenerated via `npx cap sync`)
ios/

# Traefik ACME storage (contains private cert data)
acme.json


# ACME storage bind mount — never commit the actual cert/key data, only the dir.
traefik/acme/acme.json
